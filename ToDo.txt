ToDo: track down the bug that leads to bad values in the BodyCollisionInfo objects and makes World.BodyCollisionInfo blow up.

Create draggable bodies so the test worlds are more interactive.

Does VectorTools.isCCW even need to be fixed? Unit test is currently commented out.